# @package _global_

defaults:
  - override /algorithm: base_algorithm

epochs: 2000
env:
  n_pairs: 10000

exp_name: p0201_baseline

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      seed: 0, 1, 2, 3


